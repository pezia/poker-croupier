poker-croupier Java player
==============

This is a maven project so you can import it to your favourite IDE. To start a test run :

`mvn test`

to start up a player for real use run :

 `mvn compile exec:java`