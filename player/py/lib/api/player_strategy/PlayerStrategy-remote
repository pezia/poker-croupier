#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.9.1)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

from player_strategy import PlayerStrategy
from player_strategy.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  string name()'
  print '  i64 bet_request(i64 pot, BetLimits limits)'
  print '  void competitor_status(Competitor competitor)'
  print '  void bet(Competitor competitor, Bet bet)'
  print '  void hole_card(Card card)'
  print '  void community_card(Card card)'
  print '  void showdown(Competitor competitor,  cards, HandDescriptor hand)'
  print '  void winner(Competitor competitor, i64 amount)'
  print '  void shutdown()'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = PlayerStrategy.Client(protocol)
transport.open()

if cmd == 'name':
  if len(args) != 0:
    print 'name requires 0 args'
    sys.exit(1)
  pp.pprint(client.name())

elif cmd == 'bet_request':
  if len(args) != 2:
    print 'bet_request requires 2 args'
    sys.exit(1)
  pp.pprint(client.bet_request(eval(args[0]),eval(args[1]),))

elif cmd == 'competitor_status':
  if len(args) != 1:
    print 'competitor_status requires 1 args'
    sys.exit(1)
  pp.pprint(client.competitor_status(eval(args[0]),))

elif cmd == 'bet':
  if len(args) != 2:
    print 'bet requires 2 args'
    sys.exit(1)
  pp.pprint(client.bet(eval(args[0]),eval(args[1]),))

elif cmd == 'hole_card':
  if len(args) != 1:
    print 'hole_card requires 1 args'
    sys.exit(1)
  pp.pprint(client.hole_card(eval(args[0]),))

elif cmd == 'community_card':
  if len(args) != 1:
    print 'community_card requires 1 args'
    sys.exit(1)
  pp.pprint(client.community_card(eval(args[0]),))

elif cmd == 'showdown':
  if len(args) != 3:
    print 'showdown requires 3 args'
    sys.exit(1)
  pp.pprint(client.showdown(eval(args[0]),eval(args[1]),eval(args[2]),))

elif cmd == 'winner':
  if len(args) != 2:
    print 'winner requires 2 args'
    sys.exit(1)
  pp.pprint(client.winner(eval(args[0]),eval(args[1]),))

elif cmd == 'shutdown':
  if len(args) != 0:
    print 'shutdown requires 0 args'
    sys.exit(1)
  pp.pprint(client.shutdown())

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
